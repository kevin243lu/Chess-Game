import pygame

class Square:
    def __init__(self, x, y, width, height):
        self.x = x
        self.y = y
        self.width = width
        self.height = height
        self.pos = (x, y)
        self.color = if (x + y) % 2 == 0:

if (x + y) % 2 == 0:
    color = 'light'
else:
    color = 'dark'